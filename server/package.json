{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "core": "../core",
    "endpoints": "../endpoints",
    "express": "^4.14.0",
    "settings": "../settings",
    "swagger-tools": "^0.10.1"
  },
  "bundledDependencies" : [
    "core", "endpoints", "express", "settings", "swagger-tools"
  ]
}
